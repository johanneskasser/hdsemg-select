PyQt5>=5.15.0
pyqt5-tools>=5.15.9.3.3 ; sys_platform == 'win32'
matplotlib>=3.4.0
numpy>=1.21.0
scipy>=1.7.0
requests>=2.30.0
# 0.14.1 ships global_parameters.global_amplitude and quality, which the
# Global Amplitude QC step needs. EMGFile.unit (shared#53/#54) is used when
# present and assumed otherwise, so a newer release improves QC but is not
# required. NOTE: the published 0.14.2 wheel does NOT contain units.py --
# its tag predates the merge -- so bump this only once a release does.
hdsemg-shared>=0.14.1
pefile>=2023.2.7 ; sys_platform != 'win32'
