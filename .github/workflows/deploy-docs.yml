name: 📦 Build & Deploy Docs

on:
  push:
    paths:
      - 'mkdocs.yml'
      - 'docs/**'

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Fetch all history for accurate versioning

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Set package Version
        run: echo "PACKAGE_VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_ENV

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install mkdocs mkdocs-material  mkdocstrings[python-legacy]>=0.18 mike

      - name: Build with MkDocs
        run: mkdocs build --strict

 # ==== Deployment für main (alias „latest“) ====
      - name: Deploy docs for branch main
        #if: github.ref == 'refs/heads/main' TODO: REMOVE thIS COMMENT
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Mike erzeugt einen Ordner „latest/“ im gh-pages-Branch
          mike deploy latest --push --update-aliases

      # ==== Deployment für Tags (z. B. v1.2.0) ====
      - name: Deploy docs for tagged release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Extrahiere z.B. „v1.2.0“ → „1.2.0“
          VERSION="${GITHUB_REF#refs/tags/v}"
          # Deploye unter dem Ordner „1.2.0/“
          mike deploy "$VERSION" --push
          # Setze diese Version als Default (optional, falls neueste Version immer direkt geöffnet werden soll)
          mike set-default "$VERSION"
